<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hello.mapper.EventMapper">

    <resultMap id="EventMap" type="com.example.hello.entity.Event">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="allDay" column="all_day"/>
        <result property="location" column="location"/>
        <result property="note" column="note"/>
        <result property="recurrence" column="recurrence"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="EventColumns">
        id, user_id, title, start_time, end_time, all_day, location, note,
        recurrence, status, create_time, update_time
    </sql>

    <sql id="ListWhere">
        WHERE user_id = #{userId}
          AND status = 1
        <if test="from != null and to != null">
            AND start_time &lt; #{to}
            AND end_time &gt; #{from}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%')
                 OR location LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <insert id="insert" parameterType="com.example.hello.entity.Event"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `event` (
            user_id, title, start_time, end_time, all_day, location, note,
            recurrence, status, create_time, update_time
        ) VALUES (
            #{userId}, #{title}, #{startTime}, #{endTime}, #{allDay}, #{location}, #{note},
            #{recurrence}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateSelective" parameterType="com.example.hello.entity.Event">
        UPDATE `event`
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="allDay != null">all_day = #{allDay},</if>
            <if test="location != null">location = #{location},</if>
            <if test="note != null">note = #{note},</if>
            <if test="recurrence != null">recurrence = #{recurrence},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id} AND user_id = #{userId} AND status = 1
    </update>

    <update id="updateStatus">
        UPDATE `event`
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <select id="selectByIdAndUserId" resultMap="EventMap">
        SELECT <include refid="EventColumns"/>
        FROM `event`
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectConflicts" resultType="com.example.hello.dto.event.ConflictEventVO">
        SELECT id, title, start_time AS startTime, end_time AS endTime
        FROM `event`
        WHERE user_id = #{userId}
          AND status = 1
          AND start_time &lt; #{endTime}
          AND end_time &gt; #{startTime}
        <if test="excludeId != null">
            AND id &lt;&gt; #{excludeId}
        </if>
        ORDER BY start_time ASC
    </select>

    <select id="countByQuery" parameterType="com.example.hello.dto.event.EventQueryParam"
            resultType="long">
        SELECT COUNT(*)
        FROM `event`
        <include refid="ListWhere"/>
    </select>

    <select id="selectPageByQuery" parameterType="com.example.hello.dto.event.EventQueryParam"
            resultMap="EventMap">
        SELECT <include refid="EventColumns"/>
        FROM `event`
        <include refid="ListWhere"/>
        ORDER BY start_time ASC
        LIMIT #{offset}, #{pageSize}
    </select>

</mapper>
